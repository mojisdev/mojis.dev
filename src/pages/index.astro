---
import { client } from "../lib/client";

export const prerender = false;

const { data: version, error } = await client.GET("/api/v1/versions/latest");

if (error != null) {
  throw new Error(`failed to fetch versions: ${error}`);
}

return Astro.rewrite(`/${version.emoji_version}`);
---
